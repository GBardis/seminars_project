<div >
  <div class="container">
    <div class="page-header" >
      <h1>Seminars Search</h1>
    </div>
  </div>
</div>
<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Seminars Search",root_url,class: "navbar-brand"%>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to"Home",seminars_path%></li>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#"> Find Seminar
            <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% Category.all.each do |category|%>
              <li> <%= link_to category.name, category_seminars_path(category)%></li>
            <%end%>
          </ul>
        </li>
        <% if current_user.organizer? || current_user.admin?%>
          <li><%= link_to "New Seminar",new_seminar_path%></li>
        <%end%>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to  signup_path do%>
            <label>Sign Up</label>  <i class="fa fa-sign-in" aria-hidden="true"></i>
          <%end%>
        </li>
        <li>
          <%= link_to login_path  do%>
            <label>Sign In</label>  <span class="glyphicon glyphicon-user"></span>
          <%end%>
        </li>
        <% if logged_in? %>
          <li>
            <%= link_to  logout_path, method: :delete do %>
              <label>Εξοδος</label>  <i class="fa fa-sign-out" aria-hidden="true"></i>
            <%end%>
          </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>
